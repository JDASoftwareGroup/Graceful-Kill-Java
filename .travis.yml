os: windows
# workaround
language: c
install:
  - choco install adoptopenjdk8 -y
  - refreshenv
  - cd
  - dir
  - cd JDASoftwareGroup/Graceful-Kill-Java
  - mvnw install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
script:
  - mvnw test